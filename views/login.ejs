<!-- File: views/login.ejs -->
<!DOCTYPE html>
<html class="dark">
<head>
  <title>Admin Login · mee.harshwardhan</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body class="bg-gray-950 text-white font-sans flex items-center justify-center min-h-screen">
  <div class="bg-gray-900 p-8 rounded-xl shadow-lg w-full max-w-md">
    <h2 class="text-3xl font-semibold text-sky-400 mb-6 text-center">Admin Login</h2>

    <% if (typeof error !== 'undefined') { %>
      <p class="text-red-500 text-center mb-4"><%= error %></p>
    <% } %>

    <form action="/admin-harsh-login" method="POST" class="space-y-4">
      <div>
        <label class="block text-sm text-gray-400 mb-1">Username</label>
        <input type="text" name="username" required class="w-full px-4 py-2 rounded-md bg-gray-800 text-white border border-gray-700 focus:outline-none focus:ring-2 focus:ring-sky-500">
      </div>
      <div>
        <label class="block text-sm text-gray-400 mb-1">Password</label>
        <input type="password" name="password" required class="w-full px-4 py-2 rounded-md bg-gray-800 text-white border border-gray-700 focus:outline-none focus:ring-2 focus:ring-sky-500">
      </div>
      <button type="submit" class="w-full py-2 mt-4 bg-sky-500 hover:bg-sky-600 rounded-md font-medium">Login</button>
    </form>
  </div>
</body>
</html>
